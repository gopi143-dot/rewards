spring:
  application:
    name: rewards-api

  datasource:
    url: jdbc:oracle:thin:@localhost:1522/ORCLPDB   # adjust host/port/service name
    username: rewards_user
    password: Admin123
    driver-class-name: oracle.jdbc.OracleDriver

  jpa:
    hibernate:
      ddl-auto: none   # Prevent Hibernate from altering schema
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.OracleDialect

